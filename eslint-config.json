{
    "plugins": [
        "regru"
    ],
    "rules": {
        "regru/no-space-in-parens": [
            "error",
            "always",
            {
                "exceptions": ["(STRING)"]
            }
        ]
    }
}
